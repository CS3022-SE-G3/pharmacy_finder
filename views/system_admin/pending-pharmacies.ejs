<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title><%= pageTitle %></title>
    <meta name="description" content="">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="">
</head>
<body>
<h2>Approval Pending Pharmacies</h2>
    <main>
        <% if (pendingPharmacies.length > 0) { %>
            <div>
                <table>
                    <thead>
                    <th>Pharmacy ID</th>
                    <th>Name</th>
                    <th>Address</th>
                    <th>Email</th>
                    <th>Contact No</th>
                    <th></th>
                    <th></th>
                    </thead>
                    <tbody>
                <% for (let pendingPharmacy of pendingPharmacies) { %>
                <tr>
                    <td><%=pendingPharmacy.pharmacy_id%></td>
                    <td><%=pendingPharmacy.name%></td>
                    <td><%=pendingPharmacy.address%></td>
                    <td><%=pendingPharmacy.email%></td>
                    <td><%=pendingPharmacy.contact_no%></td>
                    <td>
                        <a href="/system_admin/pharmacy/view/<%=pendingPharmacy.pharmacy_id%>">View</a>
                    </td>
                    <td>
                        <form action="/system_admin/pharmacy/approve" method="post">
                            <input type="hidden" name="pharmacyId" value="<%= pendingPharmacy.pharmacy_id %>">
                            <button type="submit">Approve</button>
                        </form>
                    </td>
                </tr>
                <%}%>
                    </tbody>
                </table>
            </div>
        <% } else { %>
            <h1>No Pending Pharmacies!</h1>
        <% } %>
    </main>
</body>
</html>
